package voting;

import java.util.concurrent.Callable;

public class VoteProcessRunner implements Callable<VotingResult> {

    @Override
    public VotingResult call() throws Exception {
        return null;
    }
}
